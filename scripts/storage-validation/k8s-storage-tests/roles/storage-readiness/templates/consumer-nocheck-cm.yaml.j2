apiVersion: v1
kind: ConfigMap
metadata:
  name: consumer-nocheck-cm
data:
  consume.sh: |
    echo "Reading checksums"
    md5sum $FILE_NAME > /mnt/data/consumer.md5
    checksum=$(cat /mnt/data/consumer.md5)
    echo "current checksum $checksum"
