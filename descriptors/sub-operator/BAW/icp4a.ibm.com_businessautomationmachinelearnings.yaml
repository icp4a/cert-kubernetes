###############################################################################
#
# Licensed Materials - Property of IBM
#
# (C) Copyright IBM Corp. 2024. All Rights Reserved.
#
# US Government Users Restricted Rights - Use, duplication or
# disclosure restricted by GSA ADP Schedule Contract with IBM Corp.
#
###############################################################################
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: businessautomationmachinelearnings.icp4a.ibm.com
  labels:
    app.kubernetes.io/name: ibm-dba-workflowruntime
    app.kubernetes.io/instance: ibm-dba-workflowruntime
    app.kubernetes.io/managed-by: ibm-dba-workflowruntime
    release: 24.0.0
spec:
  group: icp4a.ibm.com
  names:
    kind: BusinessAutomationMachineLearning
    listKind: BusinessAutomationMachineLearningList
    plural: businessautomationmachinelearnings
    shortNames:
    - baml
    singular: businessautomationmachinelearning
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BusinessAutomationMachineLearning is the Schema for the businessautomationmachinelearnings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BusinessAutomationMachineLearningSpec defines the desired
              state of BusinessAutomationMachineLearning
            properties:
              app_version:
                description: |-
                  ! (Be consistent with the top operator)
                  The version of the current release.
                type: string
              deployment_platform:
                default: OCP
                description: |-
                  ! From ICP4ACluster shared_configuration.sc_deployment_platform
                  The platform to be deployed specified by the user. Possible values are: OCP, ROKS and other.
                enum:
                - OCP
                - ROKS
                - other
                type: string
              deployment_type:
                default: Production
                description: |-
                  ! From ICP4ACluster shared_configuration.sc_deployment_type
                  The deployment type as selected by the user. Possible values are: Starter and Production.
                enum:
                - Starter
                - Production
                type: string
              intelligent_task_prioritization:
                properties:
                  admin_secret_name:
                    default: ""
                    type: string
                  autoscaling:
                    description: automatically scale pod server configuration
                    properties:
                      enabled:
                        default: false
                        description: If true, pods are automatically scaled within
                          the specified range
                        type: boolean
                      max_replicas:
                        default: 3
                        description: Maximum pods that can be scaled up to (must be
                          greater than MinReplicas) by horizontal pod auto scaler.
                        format: int32
                        type: integer
                      min_replicas:
                        default: 2
                        description: Minimum pods that can be scaled down to by horizontal
                          pod auto scaler.
                        format: int32
                        type: integer
                      target_cpu_utilization_percentage:
                        default: 80
                        description: Percent threshold on average CPU usage by pods
                          for triggering horizontal scaling
                        format: int32
                        type: integer
                    type: object
                  bai_prefix:
                    default: icp4ba-bai
                    type: string
                  custom_annotations:
                    additionalProperties:
                      type: string
                    description: Values in this field will be used as annotations
                      in all generated pods
                    type: object
                  custom_labels:
                    additionalProperties:
                      type: string
                    description: Values in this field will be used as labels in all
                      generated pods
                    type: object
                  deployment_profile_size:
                    default: small
                    description: Deployment profile size setting
                    enum:
                    - small
                    - medium
                    - large
                    type: string
                  gunicorn_log_level:
                    default: info
                    type: string
                  image:
                    default:
                      pull_policy: IfNotPresent
                      repository: cp.icr.io/cp/cp4a/baw/bui-task-prioritization
                    description: Image setting for Intelligent Task Prioritization
                      container
                    properties:
                      image_pull_secrets:
                        items:
                          type: string
                        type: array
                      pull_policy:
                        default: IfNotPresent
                        description: image pull policy, default is "IfNotPresent"
                        enum:
                        - IfNotPresent
                        - Always
                        - Never
                        type: string
                      repository:
                        description: image repository URL
                        type: string
                      tag:
                        description: image tag
                        type: string
                    type: object
                  itp_server_log_level:
                    default: info
                    type: string
                  node_affinity:
                    description: Node affinity configurations
                    properties:
                      custom_node_selector_match_expression:
                        description: Added in node selector match expressions. It
                          accepts array list inputs. You can assign multiple selector
                          match expressions except (kubernetes.io/arch).
                        items:
                          description: |-
                            A node selector requirement is a selector that contains values, a key, and an operator
                            that relates the key and values.
                          properties:
                            key:
                              description: The label key that the selector applies
                                to.
                              type: string
                            operator:
                              description: |-
                                Represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                              type: string
                            values:
                              description: |-
                                An array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. If the operator is Gt or Lt, the values
                                array must have a single element, which will be interpreted as an integer.
                                This array is replaced during a strategic merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      deploy_arch:
                        default:
                        - amd64
                        - s390x
                        - ppc64le
                        description: Values in this field will be used as kubernetes.io/arch
                          selector values. Default values are ['amd64','s390x','ppc64le']
                        items:
                          type: string
                        type: array
                    type: object
                  readiness_probe:
                    default:
                      failure_threshold: 6
                      initial_delay_seconds: 40
                      period_seconds: 5
                      success_threshold: 1
                      timeout_seconds: 5
                    description: Readiness Probe setting
                    properties:
                      failure_threshold:
                        default: 6
                        description: Minimum consecutive failures for the probe to
                          be considered failed after having succeeded.
                        format: int32
                        minimum: 1
                        type: integer
                      initial_delay_seconds:
                        default: 240
                        description: Number of seconds after the container has started
                          before liveness probes are initiated.
                        format: int32
                        type: integer
                      period_seconds:
                        default: 5
                        description: How often (in seconds) to perform the probe.
                        format: int32
                        minimum: 1
                        type: integer
                      success_threshold:
                        default: 1
                        description: Minimum consecutive successes for the probe to
                          be considered successful after it failed.
                        format: int32
                        minimum: 1
                        type: integer
                      timeout_seconds:
                        default: 5
                        description: Number of seconds after which the probe times
                          out.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  replicas:
                    format: int32
                    type: integer
                  resources:
                    description: Resource configuration
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  resources_init:
                    description: Init Resource configuration
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  retrain_model_schedule:
                    default: '* 3 * * 0'
                    description: |-
                      Linux cron schedule expression used to trigger Intelligent Task Prioritization server to train model using data retrieved from Business Automation Insights server
                      default value is '* 3 * * 0' every Sunday at 3AM UTC
                      for examples:
                      * 3 * * * : everyday at 3AM UTC
                      */50 * * * * : every 50 minutes
                    type: string
                  search_engine:
                    description: External search engine configuration used by IBM
                      Business Automation Insight Server
                    properties:
                      dynamic_generate_connection_info:
                        default: false
                        description: |-
                          when integrating Business Automation Insights server and its pod is deployed in same project alone with Intelligent Task Prioritization server pod,
                          Set value to true, operator retrieves search engine connection information automatically
                          Default value is false, operator always uses connection information provided, even Business Automation Insights server pod and Intelligent Task Prioritization server pod are in same project
                        type: boolean
                      endpoint:
                        description: |-
                          An search engine endpoint URL in format of https://hostname:port for connection to the Elasticsearch cluster
                          for example 'https://iaf-system-es-bai.apps.bai.cp.fyre.ibm.com:443', port value can be omitted when it is '443'
                          This parameter is mandatory when enabling remote IBM Business Automation Insight connection
                        type: string
                      secret_name:
                        description: |-
                          Search engine requires authentication and uses SSL communications, the value of this parameter
                          must provide the name of a Kubernetes secret that holds the following keys as base64-encoded strings:
                          username: search engine username
                          password: search engine password
                          ca.crt: server certificate for SSL communications
                          This parameter is mandatory when enabling remote IBM Business Automation Insight connection
                        type: string
                    type: object
                  seccomp_profile:
                    description: Optional setting for secure computing mode (seccomp)
                      profile for Intelligent Task Prioritization container.
                    properties:
                      localhost_profile:
                        description: The custom profile of secure computing mode.
                        type: string
                      type:
                        description: The type of secure computing mode.
                        enum:
                        - RuntimeDefault
                        - Unconfined
                        - Localhost
                        type: string
                    type: object
                  storage:
                    description: Storage configuration
                    properties:
                      existing_pvc_for_logstore:
                        default: ""
                        description: Persistent volume claim (PVC) for logs.
                        type: string
                      existing_pvc_for_trained_pipelines:
                        default: ""
                        description: Persistent volume claim (PVC) for dump files.
                        type: string
                      size_for_logstore:
                        default: 1Gi
                        description: Minimum size of the persistent volume (PV) that
                          is mounted as the log store.
                        type: string
                      size_for_trained_pipelines:
                        default: 10Gi
                        description: Minimum size of the persistent volume (PV) that
                          is mounted as the dump store.
                        type: string
                      use_dynamic_provisioning:
                        default: true
                        description: Set to true to use dynamic storage provisioning.
                          If set to false, you must set existing_pvc_for_logstore
                          and existing_pvc_for_trained_pipelines.
                        type: boolean
                    type: object
                  tls_secret_name:
                    default: ""
                    description: |-
                      If you want to use a customized Intelligent Task Prioritization TLS certificate, ensure it is signed by the CA in root_ca_secret.
                      If you do not want to use a customized Workflow Server TLS certificate, leave it empty
                    type: string
                type: object
              is_hyper_edition:
                default: false
                type: boolean
              license:
                description: |-
                  INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                  Must exist to accept the IBM license.
                properties:
                  accept:
                    type: boolean
                required:
                - accept
                type: object
              restricted_internet_access:
                default: false
                description: |-
                  ! From ICP4ACluster shared_configuration.sc_egress_configuration.restricted_internet_access
                  Enable or disable egress access to external systems. Default will be false if not defined.
                  true: Business Automation Machine Learning pods will not have access to any external systems unless a custom, curated egress network policy or multiple network policies with specific 'matchLabels' are created. Please refer to documentation for more detail.
                  false: All Business Automation Machine Learning pods will have unrestricted network access to external systems.
                type: boolean
              root_ca_secret:
                default: icp4a-root-ca
                description: '! From ICP4ACluster shared_configuration.root_ca_secret'
                type: string
              run_as_user:
                description: |-
                  ! From ICP4ACluster shared_configuration.run_as_user - (Be consistent with the top operator)
                  For non-OCP (e.g., CNCF platforms such as AWS, GKE, etc), this parameter is optional.
                  It is not supported on OCP and ROKS.
                  The UID to run the entrypoint of the container process.
                  Defaults to user specified in image metadata if unspecified.
                  May also be set in PodSecurityContext.  If set in both, the value specified in this field takes precedence.
                format: int64
                type: integer
              sc_is_multiple_az:
                default: false
                type: boolean
              storage_configuration:
                description: |-
                  ! From ICP4ACluster shared_configuration
                  Specify the storage class name for different storage types.
                properties:
                  dynamic_storage_classname:
                    type: string
                  fast_file_storage_classname:
                    type: string
                  medium_file_storage_classname:
                    type: string
                  slow_file_storage_classname:
                    type: string
                type: object
              workforce_insights:
                properties:
                  admin_secret_name:
                    default: ""
                    type: string
                  autoscaling:
                    description: automatically scale pod server configuration
                    properties:
                      enabled:
                        default: false
                        description: If true, pods are automatically scaled within
                          the specified range
                        type: boolean
                      max_replicas:
                        default: 3
                        description: Maximum pods that can be scaled up to (must be
                          greater than MinReplicas) by horizontal pod auto scaler.
                        format: int32
                        type: integer
                      min_replicas:
                        default: 2
                        description: Minimum pods that can be scaled down to by horizontal
                          pod auto scaler.
                        format: int32
                        type: integer
                      target_cpu_utilization_percentage:
                        default: 80
                        description: Percent threshold on average CPU usage by pods
                          for triggering horizontal scaling
                        format: int32
                        type: integer
                    type: object
                  bai_prefix:
                    default: icp4ba-bai
                    type: string
                  baml_bai_time_series_index:
                    type: string
                  baml_wfi_elastic_index:
                    type: string
                  custom_annotations:
                    additionalProperties:
                      type: string
                    description: Values in this field will be used as annotations
                      in all generated pods
                    type: object
                  custom_labels:
                    additionalProperties:
                      type: string
                    description: Values in this field will be used as labels in all
                      generated pods
                    type: object
                  deployment_profile_size:
                    default: small
                    description: Deployment profile size setting
                    type: string
                  image:
                    default:
                      pull_policy: IfNotPresent
                      repository: cp.icr.io/cp/cp4a/baw/workforce-insights
                    description: Image setting for Intelligent Task Prioritization
                      container
                    properties:
                      image_pull_secrets:
                        items:
                          type: string
                        type: array
                      pull_policy:
                        default: IfNotPresent
                        description: image pull policy, default is "IfNotPresent"
                        enum:
                        - IfNotPresent
                        - Always
                        - Never
                        type: string
                      repository:
                        description: image repository URL
                        type: string
                      tag:
                        description: image tag
                        type: string
                    type: object
                  node_affinity:
                    description: Node affinity configurations
                    properties:
                      custom_node_selector_match_expression:
                        description: Added in node selector match expressions. It
                          accepts array list inputs. You can assign multiple selector
                          match expressions except (kubernetes.io/arch).
                        items:
                          description: |-
                            A node selector requirement is a selector that contains values, a key, and an operator
                            that relates the key and values.
                          properties:
                            key:
                              description: The label key that the selector applies
                                to.
                              type: string
                            operator:
                              description: |-
                                Represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                              type: string
                            values:
                              description: |-
                                An array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. If the operator is Gt or Lt, the values
                                array must have a single element, which will be interpreted as an integer.
                                This array is replaced during a strategic merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      deploy_arch:
                        default:
                        - amd64
                        - s390x
                        - ppc64le
                        description: Values in this field will be used as kubernetes.io/arch
                          selector values. Default values are ['amd64','s390x','ppc64le']
                        items:
                          type: string
                        type: array
                    type: object
                  readiness_probe:
                    default:
                      failure_threshold: 6
                      initial_delay_seconds: 40
                      period_seconds: 5
                      success_threshold: 1
                      timeout_seconds: 5
                    description: Readiness Probe setting
                    properties:
                      failure_threshold:
                        default: 6
                        description: Minimum consecutive failures for the probe to
                          be considered failed after having succeeded.
                        format: int32
                        minimum: 1
                        type: integer
                      initial_delay_seconds:
                        default: 240
                        description: Number of seconds after the container has started
                          before liveness probes are initiated.
                        format: int32
                        type: integer
                      period_seconds:
                        default: 5
                        description: How often (in seconds) to perform the probe.
                        format: int32
                        minimum: 1
                        type: integer
                      success_threshold:
                        default: 1
                        description: Minimum consecutive successes for the probe to
                          be considered successful after it failed.
                        format: int32
                        minimum: 1
                        type: integer
                      timeout_seconds:
                        default: 5
                        description: Number of seconds after which the probe times
                          out.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  replicas:
                    format: int32
                    type: integer
                  resources:
                    description: Resource configuration
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  resources_init:
                    description: Init Resource configuration
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  seccomp_profile:
                    description: Optional setting for secure computing mode (seccomp)
                      profile for Workforce Insight server.
                    properties:
                      localhost_profile:
                        description: The custom profile of secure computing mode.
                        type: string
                      type:
                        description: The type of secure computing mode.
                        enum:
                        - RuntimeDefault
                        - Unconfined
                        - Localhost
                        type: string
                    type: object
                  storage:
                    description: Storage configuration
                    properties:
                      existing_pvc_for_logstore:
                        default: ""
                        description: Persistent volume claim (PVC) for logs.
                        type: string
                      size_for_logstore:
                        default: 1Gi
                        description: Minimum size of the persistent volume (PV) that
                          is mounted as the log store.
                        type: string
                      use_dynamic_provisioning:
                        default: true
                        description: Set to true to use dynamic storage provisioning.
                          If set to false, you must set existing_pvc_for_logstore
                          and existing_pvc_for_trained_pipelines.
                        type: boolean
                    type: object
                  tls_secret_name:
                    default: ""
                    description: |-
                      If you want to use a customized Workforce Insights TLS certificate, ensure it is signed by the CA in root_ca_secret.
                      If you do not want to use a customized Workflow Server TLS certificate, leave it empty
                    type: string
                type: object
            required:
            - license
            type: object
          status:
            description: BusinessAutomationMachineLearningStatus defines the observed
              state of BusinessAutomationMachineLearning
            properties:
              components:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                properties:
                  itp:
                    properties:
                      deployment:
                        type: string
                      service:
                        type: string
                    type: object
                  wfi:
                    properties:
                      deployment:
                        type: string
                      service:
                        type: string
                    type: object
                type: object
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              managedResources:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
