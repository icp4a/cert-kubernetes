apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: modelgateway.modelgateway.cpd.ibm.com
  labels:
    component-id: modelgateway
    icpdsupport/addOnId: model_gateway
spec:
  conversion:
    strategy: None
  group: modelgateway.cpd.ibm.com
  names:
    kind: modelgateway
    listKind: modelgatewayList
    plural: modelgateway
    singular: modelgateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The desired version of Model Gateway
      jsonPath: .spec.version
      name: Version
      type: string
    - description: The actual version of Model Gateway
      jsonPath: .status.versions.reconciled
      name: Reconciled
      type: string
    - description: The status of Model Gateway
      jsonPath: .status.modelgatewayStatus
      name: Status
      type: string
    - description: Progress percentage
      jsonPath: .status.progress
      name: Percent
      type: string
    - description: The age of Model Gateway
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Model Gateway is the Schema for the modelgateway API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of modelgateway
            properties:
              blockStorageClass:
                type: string
              docker_registry_prefix:
                type: string
              fileStorageClass:
                type: string
              ignoreForMaintenance:
                default: false
                type: boolean
              license:
                properties:
                  accept:
                    default: false
                    type: boolean
                  license:
                    pattern: ^(Enterprise|Standard)$
                    type: string
                required:
                - accept
                - license
                type: object
              scaleConfig:
                pattern: ^(small|medium|large|small_mincpureq)$
                type: string
              shutdown:
                pattern: ^(false|true|force)$
                type: string
              storageClass:
                type: string
              storageVendor:
                pattern: ^(ocs||portworx)$
                type: string
              tls_ca_secrets:
                description: Optional list of TLS CA secret names to mount into the gateway.
                items:
                  type: string
                type: array
              version:
                type: string
            required:
            - license
            x-kubernetes-preserve-unknown-fields: true
          status:
            description: Status defines the observed state of modelgateway
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
